import lookup from "../src/modules/lookup";
import MockDiscord from "./MockDiscord";

const mockDiscord = new MockDiscord();

describe("omni lookup", () => {
    it("has a first test", () => {
        console.log(mockDiscord);
    });
});
